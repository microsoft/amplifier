# Amplifier LLM Context Entry

This file is a Markdown-friendly alias for the LLM context pack.

- Use `llms.txt` for a curated table of contents.
- Use `llms-full.txt` for the aggregated full context snapshot.

OpenCode users can load this context via `/user:llms`.

To regenerate full context:

```bash
bash scripts/generate-llms.sh
```
