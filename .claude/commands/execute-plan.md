---
description: "Deprecated — use /subagent-dev instead. Redirects to subagent-driven development."
---

# /execute-plan → /subagent-dev

This command has been merged into `/subagent-dev`, which provides:
- Same batch execution with checkpoints
- Specialized agent dispatch per task (fresh context per task)
- Two-stage review (spec compliance + code quality)

**Use `/subagent-dev` instead.**

If invoked directly, announce: "The /execute-plan command has been deprecated. Running /subagent-dev instead."

Then execute `/subagent-dev` with the same arguments.

$ARGUMENTS
