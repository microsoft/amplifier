[project]
name = "amplifier-retention-proof-fixture"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []
[project.entry-points."amplifier.modules"]
hooks-retention-proof = "amplifier_module_hooks_retention_proof:mount"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["amplifier_module_hooks_retention_proof"]
