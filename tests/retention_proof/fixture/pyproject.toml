[project]
name = "amplifier-retention-proof-fixture"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []
[project.entry-points."amplifier.modules"]
hooks-retention-proof = "retention_fixture:mount"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["retention_fixture"]
